function _ClickON_TheButton(event, arguments) {
    var myWindow = window,
        brauser = myWindow.navigator.appCodeName,
        ism = brauzyra == "Mozilla";
    if (ism) {
        alert("Yes");
    }
    else {
        alert("No");
    }
}